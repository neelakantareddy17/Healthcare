# Healthcare Backend API

A production-ready **Healthcare Management** backend built with **Node.js, Express, TypeScript, PostgreSQL, and Prisma ORM**. Supports patient self-registration, admin-managed doctors, appointment booking with mocked payments, QR-code check-in, a per-doctor daily token queue system, real-time queue updates via **Socket.IO**, and in-app notifications.

---

## Tech Stack

| Layer          | Technology                     |
|----------------|---------------------------------|
| Runtime        | Node.js (ESM, `"type": "module"`) |
| Framework      | Express 4                      |
| Language       | TypeScript (strict mode)       |
| Database       | PostgreSQL                     |
| ORM            | Prisma 7 (`prisma.config.ts`)  |
| Auth           | JWT (`jsonwebtoken`)           |
| Password hash  | bcrypt                         |
| Validation     | Zod                            |
| Realtime       | Socket.IO                      |
| QR Codes       | `qrcode`                       |

---

## Project Structure

```
backend/
│
├── prisma/
│   ├── schema.prisma        # All models, enums, relations
│   ├── seed.ts               # Seeds an Admin + sample Doctor + Department
│   └── migrations/           # Generated by `prisma migrate dev`
│
├── src/
│   ├── config/                # env.ts, db.ts (Prisma client singleton)
│   ├── controllers/           # Thin HTTP layer — request in, response out
│   ├── middlewares/           # auth, role, validate, error handling
│   ├── routes/                # Route definitions only
│   ├── services/               # ALL business logic + Prisma queries
│   ├── validators/            # Zod schemas
│   ├── utils/                 # ApiError, ApiResponse, jwt, hash, qrcode, date
│   ├── socket/                 # Socket.IO server + queue event helpers
│   ├── types/                  # Shared TS types + Express Request augmentation
│   ├── app.ts                  # Express app (middleware + route wiring)
│   └── server.ts               # HTTP + Socket.IO bootstrap
│
├── package.json
├── tsconfig.json
├── prisma.config.ts            # Prisma 7 config file (replaces package.json "prisma" key)
├── .env.example
└── README.md
```

### Layered architecture rules followed

- **Controllers** — only read `req`, call a service, and send a response via `ApiResponse`. No Prisma calls, no business rules.
- **Services** — all business logic and every Prisma query lives here.
- **Routes** — only wire up `path -> middleware -> controller`. No logic.
- **Validators** — all input validation is done with Zod, applied via the `validate` middleware before the controller runs.

---

## 1. Installation

```bash
# 1. Unzip the project and move into it
cd healthcare-backend

# 2. Install dependencies
npm install

# 3. Copy the example environment file and edit it
cp .env.example .env
```

Edit `.env` and set your PostgreSQL connection string:

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/healthcare_db?schema=public"
JWT_SECRET=replace_with_a_long_random_string
```

## 2. Database setup (Prisma)

```bash
# Generate the Prisma Client
npm run prisma:generate

# Create the database schema (creates tables via migrations)
npm run prisma:migrate
# -> you will be prompted for a migration name, e.g. "init"

# (Optional but recommended) seed an Admin + sample Doctor + Department
npm run seed
```

Seeded accounts (from `prisma/seed.ts`):

| Role   | Email                  | Password    |
|--------|-------------------------|-------------|
| Admin  | admin@healthcare.com    | Admin@123   |
| Doctor | doctor@healthcare.com   | Doctor@123  |

> Doctors and Admins **cannot self-register** — only Patients can register via `POST /api/auth/register`. The first Admin must be seeded (as above) or inserted directly into the database; that Admin then creates all further Doctor accounts through the API.

## 3. Run the project

```bash
# Development (auto-reload via tsx)
npm run dev

# Production build
npm run build
npm start
```

The API will start on `http://localhost:5000` (configurable via `PORT` in `.env`).
Health check: `GET http://localhost:5000/health`

---

## Authentication

All protected routes expect:

```
Authorization: Bearer <JWT_TOKEN>
```

JWT payload shape:

```json
{ "userId": "uuid", "role": "PATIENT | DOCTOR | ADMIN" }
```

| Rule | Details |
|---|---|
| Patients | Can self-register via `POST /api/auth/register` |
| Doctors  | **Cannot** self-register — created only by an Admin via `POST /api/doctors` |
| Admins   | **Cannot** self-register — seeded once, then manage the system |

---

## API Reference

Base URL: `/api`

### Auth
| Method | Endpoint | Auth | Description |
|---|---|---|---|
| POST | `/auth/register` | Public | Patient self-registration |
| POST | `/auth/login` | Public | Login (any role) |
| GET  | `/auth/me` | Bearer | Get current logged-in user |

**Register — request**
```json
POST /api/auth/register
{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "Passw0rd!",
  "phone": "9876543210",
  "gender": "MALE",
  "address": "12 Main St",
  "bloodGroup": "O+"
}
```
**Register — response (201)**
```json
{
  "success": true,
  "message": "Patient registered successfully",
  "data": {
    "user": { "id": "...", "name": "John Doe", "email": "john@example.com", "role": "PATIENT", "patient": { "...": "..." } },
    "token": "eyJhbGciOi..."
  }
}
```

**Login — request**
```json
POST /api/auth/login
{ "email": "john@example.com", "password": "Passw0rd!" }
```

### Departments
| Method | Endpoint | Auth |
|---|---|---|
| GET | `/departments` | Public |
| GET | `/departments/:id` | Public |
| POST | `/departments` | Admin |
| PUT | `/departments/:id` | Admin |
| DELETE | `/departments/:id` | Admin |

### Doctors
| Method | Endpoint | Auth |
|---|---|---|
| GET | `/doctors` | Public |
| GET | `/doctors/:id` | Public |
| POST | `/doctors` | Admin only — creates the User + Doctor profile |
| PUT | `/doctors/:id` | Admin |
| DELETE | `/doctors/:id` | Admin (deactivates account) |

**Create doctor — request**
```json
POST /api/doctors
Authorization: Bearer <admin_token>
{
  "name": "Dr. Alice Ray",
  "email": "alice@healthcare.com",
  "password": "Doctor@123",
  "departmentId": "<department-uuid>",
  "specialization": "Cardiologist",
  "experienceYears": 10,
  "consultationFee": 800,
  "qualification": "MBBS, MD (Cardio)"
}
```

### Patients
| Method | Endpoint | Auth |
|---|---|---|
| GET | `/patients` | Admin, Doctor |
| GET | `/patients/:id` | Bearer |
| PUT | `/patients/:id` | Self or Admin |
| DELETE | `/patients/:id` | Admin |

### Appointments
| Method | Endpoint | Auth | Notes |
|---|---|---|---|
| POST | `/appointments` | Patient | Blocked if doctor is on leave that day |
| GET | `/appointments` | Bearer | Auto-scoped: patients see their own, doctors see theirs, admin sees all. Supports `?status=&doctorId=&patientId=&date=&page=&limit=` |
| GET | `/appointments/:id` | Bearer | |
| PUT | `/appointments/:id/cancel` | Bearer | Patients cancel their own; Admin can cancel any |

**Create appointment — request**
```json
POST /api/appointments
Authorization: Bearer <patient_token>
{
  "doctorId": "<doctor-uuid>",
  "appointmentDate": "2026-08-10",
  "timeSlot": "10:00-10:15",
  "reason": "Fever and cough"
}
```
Response: appointment created with `status: "PENDING"` (awaiting payment).

### Payments (mocked — no real gateway)
| Method | Endpoint | Auth |
|---|---|---|
| POST | `/payments/:appointmentId/pay` | Patient / Admin |
| GET | `/payments/:id` | Bearer |
| GET | `/payments` | Admin |

**Pay — request**
```json
POST /api/payments/<appointmentId>/pay
{ "method": "UPI" }
```
**Pay — response**
```json
{
  "success": true,
  "message": "Payment successful, token generated",
  "data": {
    "payment": { "status": "SUCCESS", "amount": "800", "method": "UPI" },
    "queueEntry": { "tokenNumber": 3, "status": "WAITING" },
    "qrCode": "data:image/png;base64,....",
    "checkInCode": "b3f1b0e0-...."
  }
}
```
Only a `SUCCESS` payment moves the appointment to `PAID` and issues a queue token. The `qrCode` encodes the `checkInCode`, to be scanned at the clinic.

### Queue Management
| Method | Endpoint | Auth | Notes |
|---|---|---|---|
| GET | `/queue/my-queue` | Doctor | Today's queue for the logged-in doctor |
| GET | `/queue/doctor/:doctorId?date=YYYY-MM-DD` | Bearer | Queue for a specific doctor/date |
| POST | `/queue/check-in` | Bearer | Scanning the QR code sends `{ "checkInCode": "..." }`; moves appointment `PAID -> CHECKED_IN` |
| GET | `/queue/:id` | Bearer | |
| PUT | `/queue/:id/status` | Doctor/Admin | body: `{ "status": "IN_PROGRESS" \| "COMPLETED" \| "SKIPPED" \| "WAITING" }` |

**Queue rules enforced in `queue.service.ts` / `payment.service.ts`:**
- Only **paid** appointments receive a token (`createQueueEntryForAppointment` requires `status = PAID`).
- Token numbers **restart every day, per doctor** (computed from `MAX(tokenNumber)` for that doctor+date).
- One **QueueEntry per appointment** (unique constraint) — i.e., one active queue slot per booking.
- QR check-in flips `PAID -> CHECKED_IN` only (`checkInAppointment`).

### Notifications (stored in DB)
| Method | Endpoint | Auth |
|---|---|---|
| GET | `/notifications` | Bearer (own) |
| PUT | `/notifications/:id/read` | Bearer (own) |
| DELETE | `/notifications/:id` | Bearer (own) |

### Doctor Leave
| Method | Endpoint | Auth | Notes |
|---|---|---|---|
| POST | `/doctor-leaves` | Doctor | Rejected if active appointments already exist in that range |
| GET | `/doctor-leaves` | Doctor (own) / Admin (all) |
| GET | `/doctor-leaves/:id` | Bearer |
| DELETE | `/doctor-leaves/:id` | Doctor (own) / Admin |

### Dashboards
| Method | Endpoint | Auth | Returns |
|---|---|---|---|
| GET | `/dashboard/admin` | Admin | totals, revenue, appointments by status, upcoming leaves |
| GET | `/dashboard/doctor` | Doctor | today's queue, patients seen, upcoming appointments, leaves |
| GET | `/dashboard/patient` | Patient | upcoming/past appointments, unread notifications |

---

## Real-time (Socket.IO)

Connect with a JWT in the handshake:

```js
import { io } from 'socket.io-client';

const socket = io('http://localhost:5000', {
  auth: { token: '<JWT_TOKEN>' },
});

// Doctor dashboards subscribe to a specific doctor's live queue
socket.emit('queue:join', '<doctorId>');

socket.on('queue:update', (queueSnapshot) => {
  console.log('Live queue:', queueSnapshot);
});

socket.on('queue:token-called', ({ tokenNumber }) => {
  console.log('Now serving token:', tokenNumber);
});

// Every authenticated user automatically receives personal notifications
socket.on('notification:new', (notification) => {
  console.log('New notification:', notification);
});
```

Emitted from the backend:
- `queue:update` — whenever a queue entry is created/checked-in/status-changed (`src/socket/queue.socket.ts`)
- `notification:new` — whenever a notification is created for a user (`src/services/notification.service.ts`)

---

## Error Handling

All errors funnel through a single `errorHandler` middleware (`src/middlewares/error.middleware.ts`) and respond in a consistent shape:

```json
{ "success": false, "message": "Doctor is on leave on the selected date" }
```

Prisma known errors (`P2002` unique constraint, `P2025` not found) are automatically translated to `409`/`404` responses.

---

## Example Postman Collection

Import [`Healthcare.postman_collection.json`](./Healthcare.postman_collection.json) (included in this project) into Postman. It contains every endpoint above, pre-wired with a `{{baseUrl}}` variable (`http://localhost:5000/api`) and a `{{token}}` variable you can set from the login response.

---

## Notes & Assumptions

- Payments are **fully mocked** — `POST /payments/:appointmentId/pay` always succeeds (no external gateway).
- QR check-in uses a UUID `checkInCode` stored on each `Appointment`; `qrcode` renders it as a base64 PNG at payment time.
- Prisma 7's config is expressed in `prisma.config.ts` rather than a `"prisma"` key in `package.json`.
- Soft-delete pattern is used for Doctors/Patients (`isActive = false`) to preserve appointment/payment history instead of hard-deleting.
